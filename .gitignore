# Секреты — токен живёт только локально и в GitHub Secrets, в git не попадает
telegram_token.txt

# Python
__pycache__/
*.pyc

# Личные настройки Claude Code (разрешения и т.п.) — не для публичного репо
.claude/settings.local.json
